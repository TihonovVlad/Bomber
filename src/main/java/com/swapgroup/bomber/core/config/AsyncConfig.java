package com.swapgroup.bomber.core.config;

public class AsyncConfig {
// настройка пула потоков для @Async
    //@Configuration + @EnableAsync + ThreadPoolTaskExecutor (размер пула, очередь, reject policy).
}
